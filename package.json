{
  "name": "habit-tracker-api",
  "version": "1.0.0",
  "description": "API para rastreamento de hábitos",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "mocha test/**/*.test.js --timeout 10000 --exit",
    "test:unit": "mocha test/unit/**/*.test.js --timeout 10000 --exit",
    "test:integration": "mocha test/integration/**/*.test.js --timeout 10000 --exit",
    "test:external": "mocha test/external/**/*.test.js --timeout 15000 --exit",
    "test:watch": "mocha test/**/*.test.js --timeout 10000 --watch",
    "test:coverage": "nyc mocha test/**/*.test.js --timeout 10000 --exit"
  },
  "dependencies": {
    "apollo-server-express": "^3.13.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.10.0",
    "graphql": "^16.11.0",
    "joi": "^17.9.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0"
  },
  "devDependencies": {
    "chai": "^4.3.8",
    "mocha": "^10.2.0",
    "nodemon": "^3.0.1",
    "sinon": "^21.0.0",
    "supertest": "^6.3.3"
  }
}
